[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Vite reads VITE_* env vars automatically at build time.
# Set VITE_NASA_API_KEY in the Netlify UI:
#   Site settings → Environment variables → Add variable
